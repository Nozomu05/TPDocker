server.port=8080
spring.application.name=RentalService

# PHP Service Configuration
# For Docker Compose:
#php.service.url=${PHP_SERVICE_URL:http://localhost:8081}

# For Kubernetes (DNS-based service discovery):
php.service.url=${PHP_SERVICE_URL:http://phpservice-service.default.svc.cluster.local:80}
